# Exemple de fichier d'environnement
# Copiez ce fichier en .env.development et .env.production et ajustez les valeurs

# Configuration générale
NODE_ENV=development # ou production

# Configuration pour la performance
NEXT_TELEMETRY_DISABLED=1
NODE_OPTIONS="--max-old-space-size=4096"

# Optimisations
NEXT_DISABLE_CUSTOM_BABEL=1
NEXT_FORCE_SWC=1

# Cloudflare configuration
NEXT_PUBLIC_CLOUDFLARE_BASE_URL=https://map-dev.galeon.community # ou https://map.galeon.community pour la production
NEXT_PUBLIC_CLOUDFLARE_ANALYTICS_ID=votre_id_analytics_cloudflare

# Mapbox configuration (OBLIGATOIRE - ne jamais commiter de vraies valeurs)
# Créer un token sur https://account.mapbox.com/access-tokens/
# Avec restrictions: URL allowlist (*.galeon.community, localhost:3000)
# Scopes minimaux: styles:read, fonts:read, tiles:read
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here

# API configuration
NEXT_PUBLIC_API_URL=https://api-dev.galeon.community # ou https://api.galeon.community pour la production

# Version
NEXT_PUBLIC_APP_VERSION=dev # ou numéro de version pour la production